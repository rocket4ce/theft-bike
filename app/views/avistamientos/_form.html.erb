<%= form_for([@bike, @avistamiento]) do |f| %>
  <% if @avistamiento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@avistamiento.errors.count, "error") %> prohibited this avistamiento from being saved:</h2>

      <ul>
      <% @avistamiento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :photo %><br>
    <%= f.file_field :photo %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
